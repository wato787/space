# modern-css code

最小サンプルです。ブラウザで開くだけで確認できます。

## ファイル
- `index.html`
- `styles.css`

## 実行方法
1. `index.html` をブラウザで開く
2. ウィンドウ幅を変えてレイアウトが変わることを確認する

## 見どころ
- `@layer` でカスケードの順序を固定
- `@container` でコンテナ起点のレスポンシブ
- `:has()` は `@supports selector()` でガード
- `prefers-reduced-motion` による遷移の抑制
