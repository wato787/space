# modern-css tests

手動チェック用の簡易リストです。

## 手動チェック
- `code/index.html` を開くとカードが表示される
- カードを hover / focus すると、軽い浮き上がりが起きる
- ボタンにフォーカスを当てると `transform` が効く
- ウィンドウ幅を広げるとカードが2カラムに切り替わる
- `prefers-reduced-motion` を有効にすると遷移時間が短くなる
- `:has()` 対応ブラウザでは、`New` バッジがある時に枠色が強調される
