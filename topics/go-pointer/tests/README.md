# tests

学びを固定するためのテスト置き場。

- 期待する挙動（仕様）をテストで残す
- 実行手順はこのトピックの `README.md` に書く

## 手動チェック

- `code/main.go` を実行し、以下が確認できること
  - `&` でアドレス取得、`*` で参照できる
  - `nil` を参照しないように条件分岐できる
  - ポインタレシーバで構造体の値が更新される

