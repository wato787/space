# topics（トピック一覧）

このファイルは **索引** です。増えたら分類を育てていきます。

## 追加のしかた

```bash
./scripts/new-topic.sh "<topic-name>"
```

## 一覧

（ここに追記）

- `terraform` - IaC基礎、state/plan/apply、例コードまとめ
- `example-topic` - テンプレの使い方サンプル（不要なら削除OK）
- `go-pointer` - Goのポインタ基礎（`&` / `*` / `nil` / ポインタレシーバ）

