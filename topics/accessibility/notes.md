# notes

調査ログ（時系列メモ）用。

## 2026-02-07

- 原則は POUR（Perceivable / Operable / Understandable / Robust）
- まずはセマンティックHTML、ARIAは最後の手段
- 見出し/ランドマーク/スキップリンクで探索コストを下げる
- フォームは `label` / `fieldset` / `legend` を使って意味づけ
- 動的更新は `aria-live`、状態変化は `aria-pressed` などで明示

